# Agent entry

Read [.intent/state/STATE.md](.intent/state/STATE.md) before changing this package.

The root is a private development workspace. `packages/dsh-reply-model-hint/` owns the installable package, source, tests, resources and build output; root scripts own build/typecheck/setup/inspect/remove. Use README and STATE for executable commands. Preserve npm identities and runtime data when changing package paths.
